import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ChapterCard = ({ data }) => {
    
  return (
    <View>
      <Text>ChapterCard</Text>
    </View>
  );
};

export default ChapterCard;

const styles = StyleSheet.create({});
